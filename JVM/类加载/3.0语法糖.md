### 默认构造器
- 当我们的方法中没有写构造器时，编译器会默认给我调用父类的supper构造器

### 自动拆装箱
- java 编译器在编译阶段 自动给字节码 装箱 和拆箱

### 范型擦除

### 范型反射

### 可变参数

### foreach

### switch-string

### switch-enum

### 枚举

### twr(try-with-resources)
```java
try(资源变量 = 创建资源对象){
    
}catch(){

}
```
- 其中资源对象需要实现 AutoCloseable接口，例如InputStream,OutputStream,Connection,Statement,ResultSet
- 等接口都实现了AutoCloseable，使用try-with-resources可以不用写finnaly语句块，编译器会帮助生存资源代码

### 重写桥接

### 译名内部类